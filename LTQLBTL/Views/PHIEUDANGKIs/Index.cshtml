@model IEnumerable<LTQLBTL.Models.PHIEUDANGKI>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaNV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ngaydk)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaNV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ngaydk)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaPDK }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaPDK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaPDK })
        </td>
    </tr>
}

</table>
